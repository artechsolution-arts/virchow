{{- if .Values.ingress.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "virchow.fullname" . }}-ingress-webserver
  annotations:
    {{- if not .Values.ingress.className }}
    kubernetes.io/ingress.class: nginx
    {{- end }}
    cert-manager.io/cluster-issuer: {{ include "virchow.fullname" . }}-letsencrypt
    kubernetes.io/tls-acme: "true"
spec:
  {{- if .Values.ingress.className }}
  ingressClassName: {{ .Values.ingress.className }}
  {{- end }}
  rules:
    - host: {{ .Values.ingress.webserver.host }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "virchow.fullname" . }}-webserver
                port:
                  number: {{ .Values.webserver.service.servicePort }}
  tls:
    - hosts:
        - {{ .Values.ingress.webserver.host }}
      secretName: {{ include "virchow.fullname" . }}-ingress-webserver-tls
{{- end }}